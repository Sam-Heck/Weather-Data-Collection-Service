import { pool } from '../utils/MySqlClient.js'

export class weatherRepository {

    async saveCurrentWeather() {
        // pass in data object as an arg to save
    }

    async saveForecastWeather() {
        // pass in data object as an arg to save
    }
}